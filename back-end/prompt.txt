用中文回复：
请提取以下字段的信息并以严格JSON格式输出，每个字段的内容不超过50字：

**任务要求：**
1. 提取以下字段：{fields_str}。
2. 若字段未提及，填"未提及"。
3. 确保JSON格式严格正确，可被解析器解析。

**输出格式要求：**
- 使用标准JSON格式，所有字符串用双引号包围。
- 若值含双引号，需转义为\"。
- 若值含反斜杠，需转义为\\。
- 若值含换行符，使用\n表示。
- 数值与日期保持文献原始格式。
- 所有字段值总字数不超过50字。

**输出示例格式：**
```json
{
  "字段1": "具体值",
  "字段2": "未提及",
  "字段3": "含有\"引号\"的值"
}
```